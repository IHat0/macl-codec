# macl-codec-
The core software engine for the MAC-L (Machine Action Codec) protocol. Implements a high-performance, asynchronous sensory interrupt system (the "Spinal Cord") and atomic envelope parser for real-time, low-latency LLM agent control. Features 10µs queue-wipe latency and 100% grammar validation.
